format_version: 1
pipelines:
  dhruv:
    group: test-gocd
    label_template: ${COUNT}
    template: test-gocd
    materials:
      repo:
        git: git@github.com:dhruvguptaTTN/test-gocd.git
        branch: main
      command: "bash"
templates:
  test-gocd:
    stages:
      - BuildStage:
          jobs:
            - BuildJob:
                tasks:
                  - exec:
                      command: chmod
                      arguments:
                        - +x
                        - test.sh
                  - exec:
                      command: ./test.sh
 
